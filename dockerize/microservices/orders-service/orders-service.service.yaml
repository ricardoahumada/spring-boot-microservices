apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    app: orders
spec:
  selector:
    app: orders
  type: NodePort
  ports:
  - port: 9100
    targetPort: http-orders
    nodePort: 30164 #30000-32767
    protocol: TCP
    name: http-orders
