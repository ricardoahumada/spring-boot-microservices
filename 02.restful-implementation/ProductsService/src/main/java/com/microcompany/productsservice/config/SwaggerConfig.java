package com.microcompany.productsservice.config;

import org.springframework.context.annotation.Configuration;

//@Configuration
//@EnableSwagger2
public class SwaggerConfig {/*
    @Bean
    public Docket productApi() {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.microcompany.productsservice"))
                .paths(PathSelectors.any())
                .build().apiInfo(getApiInfo());
    }

    private ApiInfo getApiInfo() {
		return new ApiInfo(
			"Products API",
			"Products API Description",
			"1.0",
			"http://demo.microcompany.com/terms",
			new Contact("microcompany", "https://microcompany.com", "apis@microcompany.com"),
			"LICENSE",
			"LICENSE URL",
			Collections.emptyList()
		);
	}*/
}